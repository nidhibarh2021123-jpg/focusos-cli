# focusos-cli

A simple command-line study timer that logs focus sessions and tracks daily totals.

## Features
- Timer with custom duration
- Logs sessions to CSV
- Displays total focus time for today

## Built With
- Python (standard library only)
